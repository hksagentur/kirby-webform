type: select
label:
  *: hksagentur.webform.form.label
help:
  *: hksagentur.webform.form.help
required: true
options:
  type: query
  query: kirby.collection('forms')
  text: "{{ item.name }} ({{ item.id }})"
  value: "{{ item.id }}"
